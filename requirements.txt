# Resolved against Python 3.12 / CPU Linux in the development environment.
# Install Torch first with the index matching your platform:
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
pyarrow>=14.0
scikit-learn>=1.3
pyyaml>=6.0
pydantic>=2.0
tqdm>=4.66
networkx>=3.0
matplotlib>=3.7
sentence-transformers>=2.2
kaggle>=1.6  # used by scripts/get_data.py when a token is present

# optional: investigation interface
streamlit>=1.30
# optional: dev
pytest>=7.4
ruff>=0.4
